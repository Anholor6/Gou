class priority_scheduling{
    public:
        __uint128_t process_queue[60];

        bool schedule_process (bool yield)
        {
            while (yield != true)
            {
                
            }

            return true;
        }
};