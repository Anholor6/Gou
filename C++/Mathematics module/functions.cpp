#include <iostream>

class functions{
    public:
        double composition (__uint8192_t *function_address)
        {
            double result = 0;
            return result;
        }

        bool domain_change (double number, double base, double precision)
        {
            elementaries me = new elementaries();

            number = me.calculate_logarithm(number,base,precision);

            return changed;
        }
};